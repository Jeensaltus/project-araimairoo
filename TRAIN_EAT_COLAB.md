# เทรนโมเดลคำว่า "กิน"

## ต้องทำท่ากี่ครั้ง

รัน `collect.py` แล้วเก็บข้อมูลตามค่าเริ่มต้น:

- `eat` 150 คลิป: ทำท่า "กิน" หนึ่งครั้งต่อคลิป
- `idle` 150 คลิป: วางมือหรือทำท่าอื่นเบา ๆ โดยไม่ทำท่า "กิน"

รวม 300 คลิป คลิปละ 40 เฟรม ใช้เวลาประมาณ 15-20 นาที

ทุก ๆ 25 คลิป ให้เปลี่ยนอย่างน้อยหนึ่งอย่าง: ระยะจากกล้อง, มุมตัว, แสง, ความเร็วของท่า หรือเสื้อผ้า

## เก็บข้อมูลใน VS Code

ติดตั้งแพ็กเกจก่อนครั้งแรก:

```powershell
py -m pip install opencv-python mediapipe numpy
```

จากนั้นรัน:

```powershell
py collect.py
```

กด `Q` เพื่อหยุดได้ และรันซ้ำเพื่อเก็บต่อจากคลิปที่ยังไม่ครบ

## เทรนใน Google Colab

1. บีบอัดโฟลเดอร์ `TSL_Data` เป็น `TSL_Data.zip`
2. เปิด Google Colab แล้วอัปโหลด `TSL_Data.zip`
3. อัปโหลดหรือวางโค้ดจาก `train_eat_colab.py`
4. รันทุกเซลล์ แล้วดาวน์โหลด `tfjs_model.zip`
5. แตกไฟล์ และแทนที่ไฟล์ใน `public/models/eat/` ด้วย `model.json`, ไฟล์ `.bin` และ `labels.json`
6. รัน `npm run dev` ใหม่เพื่อทดสอบ

โมเดลมีสองผลลัพธ์: `eat` และ `idle` โดย `eat` อยู่ index 0 เพื่อให้เข้ากับหน้าเว็บปัจจุบัน
